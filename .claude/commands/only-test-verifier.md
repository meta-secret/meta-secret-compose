---
description: Run only the test-verifier subagent (Gradle tests, report pass/fail).
---

# Only test verifier

Arguments: optional scope (module, test class). Example: `/only-test-verifier :composeApp:test`

Delegate to subagent **test-verifier** with input: `$ARGUMENTS`
