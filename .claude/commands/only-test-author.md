---
description: Run only the test-author subagent to add or update tests.
---

# Only test author

Arguments: what to cover (plan snippet, file list, or changed behavior). Example: `/only-test-author Cover ViewModel X for new state Y`

Delegate to subagent **test-author** with input: `$ARGUMENTS`
